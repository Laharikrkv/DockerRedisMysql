spring:
  datasource:
    url: jdbc:mysql://person_mysql:3306/testdb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   
    show-sql: true

  cache:
    type: redis
  data:
    redis:
      host: person_redis
      port: 6379
    
server:
  port: 8080
